---
description: 改代码必须在 dev 分支上进行；发现当前是 main 时立刻切到 dev
alwaysApply: true
---

# 开发分支

本仓库日常开发只在 `dev` 上进行。

- 动手改代码前先看当前分支。若是 `main`（或其它非 `dev` 分支），立刻 `git switch dev`，不要在 `main` 上改。
- 不要把功能提交到 `main`。需要发布时用 `devops/sync-main-from-dev.sh` 把 `dev` 全量镜像到 `main` 并带上构建好的 `lib/`（`dev` 不跟踪 `lib`，`main` 额外跟踪）。
- 用户没有明确要求时，不要主动切回 `main`。
