# Insert the workbench host plugin after dsh-web-app.
# One row is enough: the node half registers tools/HTTP; dsh.client discovers the browser half.
- insert:
    - id: workbench
      name: dsh-workbench-plugin
